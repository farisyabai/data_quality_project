version: 2

sources:
  - name: raw          # Logical grouping name for raw sources
    database: neondb   # Neon database name
    schema: public     # The schema where raw data stored
    tables:
      - name: customers
      - name: orders
      - name: shipments
      - name: delivery_updates