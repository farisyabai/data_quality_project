

packages:
  # Keep dbt_utils if you still need other macros from it
  - package: dbt-labs/dbt_utils
    version: 1.3.2 
  
  # ✅ FIX: Use the officially maintained package name
  - package: metaplane/dbt_expectations 
    version: [">=0.10.0", "<1.0.0"]